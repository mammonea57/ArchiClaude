# SP2-v2b render service — Python ML inference
# Target: Apple Silicon (MPS) for dev, NVIDIA (CUDA) for prod
# Phase A model: SDXL Lightning (4-step distilled SDXL) — fits 16GB RAM
# Phase B model (when CB available): FLUX.1-dev — needs RTX 4090

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.10.0
pydantic-settings==2.6.0

# Image generation stack
torch==2.5.1
torchvision==0.20.1
diffusers==0.32.0
transformers==4.46.0
accelerate==1.2.0
safetensors==0.4.5
huggingface-hub==0.27.0

# Image processing (depth maps, canny edges, post-processing)
pillow==11.0.0
numpy==2.1.3
opencv-python-headless==4.10.0.84

# HTTP client (for fetching BM from backend)
httpx==0.28.0

# Logging + dev
python-dotenv==1.0.1

# LoRA training pipeline (modal_lora_endpoint.py + photogrammetry/style_dataset.py)
# Most are CPU-side helpers for dataset assembly ; Modal images install
# their own pinned versions of torch/diffusers in modal_lora_endpoint.py.
peft>=0.13
datasets>=3.0
imagehash>=4.3       # perceptual hash dedup in style_dataset.build_combined_dataset
bitsandbytes>=0.43; sys_platform == "linux"   # 8-bit AdamW, CUDA-only
# huggingface-hub (pinned above at 0.27.0) is used by style_dataset.py for the
# HPSv3 auth check (HF_TOKEN env / ~/.cache/huggingface/token discovery).
# CLIP zero-shot filter for style_dataset.clip_filter_archviz uses
# transformers.CLIPModel (already pinned above) — no extra dep needed.
